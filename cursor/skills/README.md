# Repo agent skills (source of truth)

Edit skills **here only**. Mirror for Cursor discovery:

```bash
npm run sync:agent-skills
```

Commits **both** `.cursor/skills/` and `.agents/skills/` after sync.

**CEO invoke guide:** [`docs/projects/cursor-invoke-cheatsheet.md`](../../docs/projects/cursor-invoke-cheatsheet.md)

**Planning chain (slash in main chat):** `/grill-me` → `/planning-session` → `/slice-planning`
